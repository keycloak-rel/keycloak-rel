name: X Keycloak Node.js Admin Client

on:
  workflow_call:
    inputs:
      gh-org:
        required: true
        type: string
      tag:
        required: true
        type: string
    secrets:
      GH_TOKEN:
        required: true

defaults:
  run:
    shell: bash

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          repository: ${{ inputs.gh-org }}/keycloak
          token: ${{ secrets.GH_TOKEN }}
          ref: ${{ inputs.tag }}

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 16
          check-latest: true
          cache: npm
          cache-dependency-path: js/package-lock.json

      - name: Install dependencies
        working-directory: js
        run: npm ci

      - name: Create package tarball
        working-directory: js/libs/keycloak-admin-client
        run: |
          npm run prepublishOnly
          npm pack
          mv -T *.tgz keycloak-nodejs-admin-client.tgz

      - name: Upload to GitHub Releases
        working-directory: js/libs/keycloak-admin-client
        run: |
          for i in `gh release view ${{ inputs.tag }} --json assets --jq '.assets[].name'`; do 
            test -f $i || gh release delete-asset ${{ inputs.tag }} $i -y
          done
          gh release upload ${{ inputs.tag }} keycloak-nodejs-admin-client.tgz --clobber
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
